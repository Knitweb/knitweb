node_modules/
dist/

# curator private keys — offline only, never commit
*.key
.knitweb-curator/

# vank-adapter output — ongecureerde concept-knits, niet committen
seeds/_drafts/

# render-output (regenereerbaar; voorbeeld-mp4 als PR-artifact)
reports/*.mp4
