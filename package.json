{
  "name": "knitweb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Field-kit: N niche-portalen uit één codebase (SPEC v0.1)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node src/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "lint": "eslint \"src/**/*.mjs\" \"tools/**/*.mjs\" \"tests/**/*.mjs\"",
    "license-check": "node tools/license-check.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "esbuild": "^0.23.1",
    "eslint": "^9.39.4"
  },
  "dependencies": {
    "@noble/curves": "^1.9.7",
    "@noble/hashes": "^1.8.0"
  }
}
